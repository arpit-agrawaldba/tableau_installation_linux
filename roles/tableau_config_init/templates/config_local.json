{
   "configEntities": {
      "gatewaySettings": {
         "_type": "gatewaySettingsType",
         "port": "{{ gateway_port }}",
         "publicHost": "{{ gateway_publichost }}",
         "publicPort": "{{ gateway_publicport }}"
      },
      "identityStore": {
         "_type": "identityStoreType",
         "type": "{{ identitystore_type }}"
      }
    },
     "configKeys": {
        "gateway.timeout": "{{ gateway_timeout }}"
     }
}
